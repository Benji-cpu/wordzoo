# Feedback Triage — 2026-05-06

## WARNING: Sandbox Egress Blocked

Both production endpoints returned HTTP 403 with reason "Host not in allowlist".
No feedback data could be retrieved for this run.

### Endpoints attempted

- `https://wordzoo.vercel.app/api/cron/nightly-routine` — HTTP 403 (host_not_allowed)
- `https://wordzoo.vercel.app/api/admin/feedback/pending` — HTTP 403 (host_not_allowed)

### Action required

The agent sandbox's egress policy does not permit outbound connections to `wordzoo.vercel.app`.
To fix this, the production host must be added to the sandbox allowlist for the scheduled agent,
or the trigger must be reconfigured to run in an environment with unrestricted egress.

## Headline

- New feedback last 24h: unknown (endpoint blocked)
- Pending total: unknown (endpoint blocked)
- Stuck mnemonics (>72h missing audio): unknown (endpoint blocked)
- Overdue SRS reviews: unknown (endpoint blocked)

## Priority items

None retrieved — endpoint blocked.

## Clusters (standard)

None retrieved — endpoint blocked.

## Noise / unclear

None retrieved — endpoint blocked.

## Recommended single-leverage fix

report-only — no fix attempted today. Both data endpoints were unreachable due to sandbox
egress restrictions (HTTP 403, "Host not in allowlist"). No feedback data was available
to evaluate for an automated fix.
