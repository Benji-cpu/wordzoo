# triage: missing digest — Vercel prepare step failed 2026-08-19

The Vercel-side `/api/cron/nightly-routine` prepare step did not produce `digests/2026-08-19.json` before this agent fired.

This is now **6 consecutive missing digests** (2026-08-14 through 2026-08-19). Check Vercel logs for the `/api/cron/nightly-routine` route. Most likely cause: `GITHUB_PAT_REPO_WRITE` is missing, expired, or has incorrect scope in Vercel environment variables.

To diagnose: `curl -H "Authorization: Bearer $CRON_SECRET" https://wordzoo.vercel.app/api/cron/nightly-routine` — inspect `errors[]` and `digestWritten` in the response.
