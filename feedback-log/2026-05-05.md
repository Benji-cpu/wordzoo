# Feedback Triage — 2026-05-05

## WARNING: Sandbox Egress Blocked

This run could not retrieve live data. Both production endpoints returned HTTP 403 "host not in allowlist" — the agent sandbox does not permit outbound requests to wordzoo.vercel.app.

### Endpoints attempted

| Endpoint | HTTP Status | Response |
|---|---|---|
| `GET /api/cron/nightly-routine` | 403 | Host not in allowlist |
| `GET /api/admin/feedback/pending` | 403 | Host not in allowlist |

### What this means

- No feedback rows were fetched or triaged.
- No rows have been marked as reviewed.
- No headline stats (new feedback, stuck mnemonics, overdue SRS reviews) are available.
- No code fix was attempted.

### Recommended action

The sandbox egress allowlist needs to include `wordzoo.vercel.app` for this agent to function. Until then, the nightly routine cannot fetch data or mark feedback as reviewed.

Options:
1. Add `wordzoo.vercel.app` to the agent sandbox egress allowlist (preferred).
2. Run the triage manually via the admin UI at `https://wordzoo.vercel.app/admin`.
3. Trigger a fresh agent run after the allowlist is updated.

## Recent Commits (for context)

The following commits shipped recently on `main` — useful context once live data is accessible:

```
55609a7 feat(cron): switch nightly-routine to Claude Code remote agent (#3)
e3b5345 docs(claude): fix admin email — profbenjo, not profbanjo (#2)
2e2d2ef fix(cron): nightly-routine references wrong column for overdue reviews
78045db chore(cron): remove obsolete remote-agent files, align README
cea699d feat(cron): add nightly-routine for cross-project digest (#1)
f46a2a1 feat(ops): daily feedback triage agent + admin-emails docs
3262842 feat(feedback): activity trail attached to app-feedback submissions
711d18c fix(nav): rounded-square active highlight in bottom nav
8238dbd fix(tutor): allow longer responses + session evaluation feedback
f32c2c9 fix(scenes): server-side sequential lock so users can't skip ahead
```

## Recommended single-leverage fix

report-only — no fix attempted today. Reason: both data-fetch endpoints were blocked by sandbox egress (HTTP 403); without feedback rows there is no confirmed symptom to act on.
