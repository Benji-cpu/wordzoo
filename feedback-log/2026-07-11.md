# triage: missing digest — Vercel prepare step failed 2026-07-11

`digests/2026-07-11.json` was not present in the repo when this agent ran at ~03:32 Bali time.

The Vercel cron job at `/api/cron/nightly-routine` (scheduled 03:27 Bali / 19:27 UTC) either did not fire or errored before writing the digest.

**This is the 5th consecutive day with a missing digest** (2026-07-07 through 2026-07-11). The pattern strongly suggests a persistent failure in the Vercel prepare step — likely `GITHUB_PAT_REPO_WRITE` is missing, expired, or has insufficient scope in Vercel env vars.

Action required: check Vercel runtime logs for `/api/cron/nightly-routine` and verify `GITHUB_PAT_REPO_WRITE` is set with `Contents: write` scope on `Benji-cpu/wordzoo`.
