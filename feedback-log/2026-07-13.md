# triage: missing digest — Vercel prepare step failed 2026-07-13

The Vercel-side `/api/cron/nightly-routine` prepare step did not write `digests/2026-07-13.json` to the repo before this agent fired.

**Pattern:** This is now the 7th consecutive missing digest (July 7–13). The last successful Vercel prepare run was 2026-07-06.

**Likely causes:**
- `GITHUB_PAT_REPO_WRITE` is expired, missing, or has wrong scope in Vercel environment variables (Contents: write on `Benji-cpu/wordzoo` required).
- The prepare route `/api/cron/nightly-routine` is erroring (5xx) — check Vercel runtime logs for that route.
- The Vercel cron itself (`27 19 * * *` UTC) may have been paused or removed.

**Action required:**
1. Log in to Vercel → WordZoo project → Settings → Environment Variables → verify `GITHUB_PAT_REPO_WRITE` is present and not expired.
2. Rotate the PAT if needed (fine-grained, scoped to `Benji-cpu/wordzoo`, `Contents: write`).
3. Curl the route manually to test: `curl -H "Authorization: Bearer $CRON_SECRET" https://wordzoo.vercel.app/api/cron/nightly-routine`
4. Check Vercel logs for the cron job history.
