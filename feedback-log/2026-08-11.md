# triage: missing digest — Vercel prepare step failed 2026-08-11

No digest file found at `digests/2026-08-11.json`. The Vercel-side `/api/cron/nightly-routine` prepare step (scheduled 19:27 UTC / 03:27 Bali) did not write the digest for today.

**Action required:** Check Vercel runtime logs for the `nightly-routine` cron route. Most likely causes:
1. `GITHUB_PAT_REPO_WRITE` missing, expired, or lacking `Contents: write` scope in Vercel environment variables.
2. Neon DB query failure (check `errors[]` in the route response).
3. Vercel cron itself failed to invoke the route.

Verify the route is healthy by curling with `Authorization: Bearer $CRON_SECRET` from a machine with outbound access to `wordzoo.vercel.app`.

_deployment ingest unreachable from sandbox_
