# triage: missing digest — Vercel prepare step failed 2026-08-02

Vercel cron `/api/cron/nightly-routine` did not write `digests/2026-08-02.json` before this agent fired.

Check Vercel runtime logs for the 19:27 UTC run. Most likely causes: `GITHUB_PAT_REPO_WRITE` expired or wrong scope, Neon connectivity issue, or the cron job missed its schedule.

> Note: missing digests are a recurring pattern (consecutive failures observed July 7–25 and Aug 1). The Vercel prepare step appears unreliable — investigate the PAT and cron configuration.
