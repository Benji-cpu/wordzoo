# Feedback Triage — 2026-09-17

## Headline
- New feedback last 24h: 0
- Pending total: 0 (138 actioned, 1 dismissed — all caught up)
- Stuck mnemonics (>72h missing audio): **1**
- Overdue SRS reviews: **183** (183 words 8d+ late, 81 phrases 8d+ late; 29 items due now)

_deployment ingest unreachable from sandbox_

## Priority items
None — no pending feedback today.

## Clusters (standard)
No pending feedback rows to cluster.

## Noise / unclear
None.

## Pedagogy signals (health metrics — no user complaints, but worth flagging)

The drills went permanent yesterday (`feat(pedagogy): drill slices permanent, legacy loop deleted`). First-day numbers:

| Cue type | Attempts | Correct | Accuracy |
|---|---|---|---|
| production | 34 | 0 | **0%** |
| phrase_production | 34 | 0 | **0%** |
| phrase_recognition | 27 | 13 | 48% |
| cloze | 21 | 0 | **0%** |
| phrase_cloze | 21 | 0 | **0%** |

**Concern**: 0% on every typed / production cue type across 110 combined attempts is suspicious for day-1 data. Could be:
1. Learners genuinely haven't had time to build production recall after the v2 flip (vocab exposure still at 95% × 1-touch, consolidation drops sharply after 3 exposures).
2. A grader regression — the drill grader or the typo-tolerance normalizer is rejecting valid answers. The normalize tests in `lib/pedagogy/normalize.test.ts` should catch this but are worth running.
3. The 0% is a snapshot artefact — if all 34 production attempts were the same few learners hitting items for the first time right after the deploy, the number will recover.

**Retention rate in learning phase**: 25% (37/148 correct). This is expected for new items but should trend up over the next 3–5 days as the easy-pass items clear out.

**Overdue backlog**: 183 words and 81 phrases are 8d+ late. This is a long-standing retention backlog (visible in prior digests), not a new regression. The due-by-likelihood queue ordering introduced in `feat(pedagogy)` should start draining it gradually.

**Checkpoint pass rate**: 0% — 1 checkpoint attempted, 0 passed (2 avg remediation loops). Single-sample noise; watch tomorrow.

**Schedule health**: Good — mean ease 2.52 (above floor), 0 leeches, 0 items over max interval.

## Recommended single-leverage fix
Report-only — no fix attempted today. The 0% production accuracy warrants monitoring but no single file fix is clear without:
(a) user complaints naming a specific symptom, or
(b) a reproduce step showing the grader rejects an answer it should accept.

If accuracy is still 0% tomorrow, the highest-leverage investigation is `lib/pedagogy/normalize.ts` (typo tolerance) and the production-cue grader in the learn flow — but that crosses the "needs investigation" threshold, not a ≤30-line single-file fix.
