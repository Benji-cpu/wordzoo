# Feedback Triage — 2026-09-05

triage: missing digest — Vercel prepare step failed 2026-09-05

The expected digest file `digests/2026-09-05.json` was not present when this agent fired.
Check Vercel runtime logs for `/api/cron/nightly-routine` (scheduled 19:27 UTC).

Most likely causes:
- `GITHUB_PAT_REPO_WRITE` missing, expired, or wrong scope in Vercel envs (most common)
- Neon DB query failure in the prepare route — inspect `errors[]` and `digestWritten` in response

To diagnose, curl the route from a local machine:
```
curl -H "Authorization: Bearer $CRON_SECRET" https://wordzoo.vercel.app/api/cron/nightly-routine
```
