# triage: missing digest — Vercel prepare step failed 2026-09-07

The Vercel-side `/api/cron/nightly-routine` did not write `digests/2026-09-07.json` before this agent fired at ~03:32 Bali time.

**To investigate:** curl the route from your Mac with `Authorization: Bearer $CRON_SECRET` and inspect `errors[]` and `digestWritten` in the response. Most common cause: `GITHUB_PAT_REPO_WRITE` missing, expired, or has incorrect scope in Vercel envs.
