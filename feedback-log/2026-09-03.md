# triage: missing digest — Vercel prepare step failed 2026-09-03

The Vercel-side `/api/cron/nightly-routine` prepare step did not write `digests/2026-09-03.json` before this agent fired.

**Action required:** Check Vercel runtime logs for the `nightly-routine` cron job. Most common causes:
- `GITHUB_PAT_REPO_WRITE` missing, expired, or wrong scope in Vercel environment variables (needs `Contents: write` on `Benji-cpu/wordzoo`)
- Neon DB down or query error (look for non-empty `errors[]` in the route response)
- The route returned 5xx — check Vercel function logs for a top-level throw

To verify: `curl -H "Authorization: Bearer $CRON_SECRET" https://wordzoo.vercel.app/api/cron/nightly-routine`
A 401 without auth = route is deployed and protected correctly.

_deployment ingest unreachable from sandbox_
