# triage: missing digest — Vercel prepare step failed 2026-06-27

No `digests/2026-06-27.json` was found in the repo. The Vercel-side `/api/cron/nightly-routine` prepare step did not run or errored before writing the digest.

**This is the 3rd consecutive missing digest** (2026-06-25, 2026-06-26, 2026-06-27). The Vercel prepare step is consistently failing.

## Next steps

1. Check Vercel runtime logs for `/api/cron/nightly-routine` — look for 5xx errors or a missing/expired `GITHUB_PAT_REPO_WRITE` env var.
2. A 401 from the GitHub Contents API write call is the most likely cause (PAT expired or wrong scope — needs `Contents: write` on `Benji-cpu/wordzoo`).
3. From a Mac with `CRON_SECRET` in env: `curl -H "Authorization: Bearer $CRON_SECRET" https://wordzoo.vercel.app/api/cron/nightly-routine` and inspect `errors[]` + `digestWritten` in the response.

_deployment ingest unreachable from sandbox_
