# triage: missing digest — Vercel prepare step failed 2026-07-30

Check Vercel logs for `/api/cron/nightly-routine`.

No `digests/2026-07-30.json` was written by the Vercel prepare step at 19:27 UTC. This is part of an ongoing pattern — the last successful digest in the repo is `digests/2026-07-18.json` (11 days ago). Previous agent sessions (2026-07-26 to 2026-07-29) also committed in detached HEAD state and those commits were never pushed to `origin/main`, which compounds the gap.

Most likely causes:
- `GITHUB_PAT_REPO_WRITE` missing, expired, or wrong scope in Vercel env vars
- The Vercel cron route (`/api/cron/nightly-routine`) is erroring before it writes the digest

Recommended action: verify `GITHUB_PAT_REPO_WRITE` in the Vercel project environment variables (fine-grained PAT scoped to `Benji-cpu/wordzoo` with `Contents: write`). If expired, rotate and redeploy. Curl the route manually: `curl -H "Authorization: Bearer $CRON_SECRET" https://wordzoo.vercel.app/api/cron/nightly-routine` and inspect the `errors[]` + `digestWritten` fields in the response.

_deployment ingest unreachable from sandbox_
