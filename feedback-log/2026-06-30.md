# triage: missing digest — Vercel prepare step failed 2026-06-30

The digest file `digests/2026-06-30.json` was not present when this agent ran at 19:32 UTC.

Check Vercel runtime logs for `/api/cron/nightly-routine` to diagnose why the prepare step did not write today's digest.

**Note:** This is the 6th consecutive day of missing digests (2026-06-25 through 2026-06-30). The last successful digest was 2026-06-24. The most likely causes are:

1. `GITHUB_PAT_REPO_WRITE` in Vercel env has expired or been revoked — the prepare step cannot write `digests/*.json` to GitHub without it.
2. The `/api/cron/nightly-routine` route itself is erroring (check Vercel function logs for 5xx).
3. The Vercel cron schedule is misconfigured or paused.

**Action required:** Verify `GITHUB_PAT_REPO_WRITE` is valid in Vercel project settings. If expired, generate a new fine-grained PAT scoped to `Benji-cpu/wordzoo` with `Contents: write` and update the env var.

_deployment ingest unreachable from sandbox_
