# triage: missing digest — Vercel prepare step failed 2026-09-16

The digest file `digests/2026-09-16.json` was not present when the nightly-routine agent fired at ~19:32 UTC.

**Root cause (systemic, ongoing):** The Vercel cron (`/api/cron/nightly-routine`) consistently runs ~12–17 minutes late vs. its `27 19 * * *` schedule. This trigger fires at `32 19 * * *`, so it reliably arrives before the Vercel commit lands. This race condition has produced a missing-digest stub every day since at least 2026-09-07 (with the exception of 2026-09-12, where a digest was available).

**Fix options (either one resolves the race):**
1. Move this Claude trigger to `52 19 * * *` (25 min buffer) — edit `.claude/agents/nightly-routine.md` and update the trigger at `trig_01Dnx4XZjFoduw1SEfio9vPy`.
2. Move the Vercel cron to `0 19 * * *` (27 min earlier) — edit `vercel.json` `crons` entry.

**Vercel-side context from most recent available digest (2026-09-15):**
- New feedback last 24h: 0 | Pending: 0
- Stuck mnemonics (>72h): 1
- Overdue SRS reviews: 183
- No errors

**Action required:** Check Vercel runtime logs for `/api/cron/nightly-routine`. Common causes: `GITHUB_PAT_REPO_WRITE` missing or expired, Neon query failure, or GitHub Contents API error. If the digest appears later, it is too late for this run.

_deployment ingest unreachable from sandbox_
