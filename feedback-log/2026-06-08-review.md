# Feedback Review — Action Register (2026-06-08)

Extensive review of all 137 `app_feedback` rows via the `feedback-review` workflow
(cluster → code cross-reference → adversarial verify) plus manual triage. All 15
open items (8 `new` + 7 `reviewed`) were addressed and marked `actioned`
(commit `cab234d`). Themes below carry the verified status from the workflow.

## Shipped this session (commit `cab234d`)

| Theme | Status | Notes |
|---|---|---|
| Mobile layout: continue button hidden / shifts / nav overlap | **fixed** | `SceneShell` middle is the scroll container; `PhraseQuiz` Continue is an always-reserved `sticky bottom-0` footer — selecting never shifts layout or hides the CTA behind the nav. Verified at 390px. |
| Learning-flow blockers (frozen, can't continue, name/gender form) | **fixed** | `LessonPersonaPrompt` raised above the nav (`z-[60]`) + scrollable + safe-area padding; the Save/Skip buttons are always reachable. |
| Review SRS: wrong counts / "all caught up" / restarts | **fixed (core)** | `getUserDueWords` now excludes `status='new'`, so the dashboard count matches the review session (was "34 due but 12 shown"). Verified 23 due ↔ 20-item session. *Residual:* dashboard vs queue still differ slightly by query limits — see "still needs work". |
| Tutor text bug: stray `[` in translation | **fixed** | `message-parser.ts` strips a truncated/unclosed trailing marker before render. |
| Drill too granular (1-word "secondary review") | **fixed** | `VocabularyBlock`/`PhraseBlock` keep ≤5 items in a single batch (no lone-item drill). Verified ("BATCH 1 · PHRASES 1–4 OF 4"). |
| Scenes too passive / want embedded conversation | **shipped, gated** | New in-scene `ConversationBlock` for the first 3 Indonesian scenes: progressive (easy select → medium type → hard AI-graded production), two-sided (reply + ask), accept-and-coach. End-of-scene tutor handoff de-emphasised. Verified full E2E. **Live for admins now; see env note for all users.** |
| Audio missing (words, phrases, broken-down components, all langs) | **fixed** | TTS key wired; seeded 641 words, 587 narrations, 285 phrases, 388 dialogues across id + pt. |
| Dashboard "preload screen" flash | **fixed** | Added `app/(app)/dashboard/loading.tsx` skeleton. |
| Feedback module: voice capture | **fixed** | Feedback modal auto-starts dictation on open (`Speak` toggle), stops + sends on Enter. |
| Tutor should speak | **fixed** | `Speak` toggle in tutor header voices each reply (browser TTS, markers stripped). |

## Still needs work (verified `partial` — out of this session's scope)

- **P1 — Broader conversation/v2 rollout:** set Vercel `PEDAGOGY_V2_SLICES` to include `conversation` (and `restructure,production,cloze`) so **non-admin** users get the new flow. Admins already have it via the allowlist.
- **P1 — Review count exactness:** make the dashboard "X due" and the review queue read the *same* source (align word/phrase limits) to remove the residual 23-vs-20 gap.
- **P1 — Missing visual mnemonics / mnemonic content quality:** some words/phrases still lack images, and regenerated mnemonics don't always "win" the read-path tiebreaker (add `created_at DESC`). Separate content effort.
- **P2 — Migrate the Review flow to the 3-zone contract:** `ReviewClient` still uses its own layout; apply the same `SceneShell` pinned-footer pattern for consistency.
- **P2 — Image display:** `MnemonicImage` sizing/cropping/expand-on-tap polish (+ `onError` handling).
- **P2 — Tutor language mix / hard-mode from message 1 / end state:** plumb `challengeMode` through session start.
- **P2 — Tutor session-complete forward CTA:** add a next-step loop on `word_review` / `returnTo`-less sessions (no dead end).
- **P2 — Speech/mic on Brave:** unify the Brave/unsupported-browser hint across both speech paths.
- **P2/P3 — UI consistency / branding / sharing; content copy (e.g. path title in English); scene length/ordering polish.**

_Generated by `.claude/workflows/feedback-review.js` + manual triage._
