# triage: missing digest — Vercel prepare step failed 2026-07-19

The digest file `digests/2026-07-19.json` was not written by the Vercel cron job (`/api/cron/nightly-routine`) before this agent fired.

**Check Vercel logs** for the `nightly-routine` cron run at ~19:27 UTC. Most likely cause: `GITHUB_PAT_REPO_WRITE` missing, expired, or has wrong scope in Vercel env vars (must be a fine-grained PAT scoped to `Benji-cpu/wordzoo` with `Contents: write`).

**Note:** This is the second day in a row of missing digests (2026-07-18 had a digest, but the preceding stretch of 2026-07-09 through 2026-07-17 were all missing). Intermittent PAT failures or cron timing issues may be responsible. Recommend verifying the PAT and cron health from your Mac:

```
curl -H "Authorization: Bearer $CRON_SECRET" https://wordzoo.vercel.app/api/cron/nightly-routine
```

No feedback data available for triage today.
