# Feedback Triage — 2026-07-26

triage: missing digest — Vercel prepare step failed 2026-07-26

The digest file `digests/2026-07-26.json` was not present in the repo when this agent fired at ~03:32 Bali time. The Vercel-side `/api/cron/nightly-routine` prepare step did not write it.

**⚠️ PERSISTENT FAILURE: This is now 8 consecutive days of missing digests (2026-07-19 through 2026-07-26).** The last successful digest was 2026-07-18.

## Recommended actions

1. Check Vercel runtime logs for `/api/cron/nightly-routine` — look for errors in the last 8 days.
2. Verify `GITHUB_PAT_REPO_WRITE` is still valid in Vercel environment variables (fine-grained PAT scoped to `Benji-cpu/wordzoo` with `Contents: write`). The PAT may have expired.
3. From your Mac, manually curl the prepare route to check the response:
   ```
   curl -H "Authorization: Bearer $CRON_SECRET" https://wordzoo.vercel.app/api/cron/nightly-routine
   ```
   Inspect `errors[]` and `digestWritten` fields in the response.
4. If the PAT is expired, regenerate it at https://github.com/settings/tokens and update `GITHUB_PAT_REPO_WRITE` in Vercel environment variables.

_deployment ingest unreachable from sandbox_
