# triage: missing digest — Vercel prepare step failed 2026-06-08

Digest file `digests/2026-06-08.json` was not present when this agent ran.

Check Vercel runtime logs for `/api/cron/nightly-routine` to diagnose — most likely cause is a missing or expired `GITHUB_PAT_REPO_WRITE` env var, or a Neon query failure. Manual curl with `Authorization: Bearer $CRON_SECRET` can confirm.
