# triage: missing digest — Vercel prepare step failed 2026-05-31

The Vercel-side cron route (`/api/cron/nightly-routine`) did not write `digests/2026-05-31.json` before this agent fired. Check Vercel runtime logs for the prepare step to diagnose the failure (likely `GITHUB_PAT_REPO_WRITE` expired, Neon down, or the cron job itself failed to trigger).
