# triage: missing digest — Vercel prepare step failed 2026-05-19

`digests/2026-05-19.json` was not present in the repo when this agent ran.

The Vercel-side `/api/cron/nightly-routine` prepare step either did not fire or errored before writing the digest. Check Vercel runtime logs for the `nightly-routine` cron job and verify that `GITHUB_PAT_REPO_WRITE` is set, unexpired, and scoped to `Contents: write` on `Benji-cpu/wordzoo`.
