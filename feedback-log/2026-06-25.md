# triage: missing digest — Vercel prepare step failed 2026-06-25

The Vercel-side `/api/cron/nightly-routine` prepare step did not write `digests/2026-06-25.json` to `main` before this agent fired. No synthesis was possible.

**Action required:** Check Vercel runtime logs for the `nightly-routine` cron at ~19:27 UTC on 2026-06-24. Most common causes:

- `GITHUB_PAT_REPO_WRITE` missing, expired, or wrong scope in Vercel env vars (needs `Contents: write` on `Benji-cpu/wordzoo`)
- Neon DB query failure (check `errors[]` in the route response)
- Vercel cron itself didn't fire (check Vercel cron logs)

Curl the route manually to diagnose:

```
curl -H "Authorization: Bearer $CRON_SECRET" https://wordzoo.vercel.app/api/cron/nightly-routine
```

Previous day (2026-06-24): 0 pending feedback, 1 stuck mnemonic, 148 overdue SRS reviews.
