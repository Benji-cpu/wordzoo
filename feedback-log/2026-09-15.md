# triage: missing digest — Vercel prepare step failed 2026-09-15

The digest file `digests/2026-09-15.json` was not present when the nightly-routine agent fired at ~19:32 UTC.

**Root cause (systemic):** The Vercel cron (`/api/cron/nightly-routine`) consistently runs ~12–17 minutes late vs. its `27 19 * * *` schedule. The most recent successful digest (`2026-09-14.json`) shows `startedAt: "2026-09-13T19:44:00.691Z"` — 17 minutes after schedule. This trigger fires at `32 19 * * *`, so it reliably arrives before the Vercel commit lands.

**Fix options (either one resolves the race):**
1. Move this Claude trigger to `52 19 * * *` (25 min buffer) — edit `.claude/agents/nightly-routine.md` and update the trigger at `trig_01Dnx4XZjFoduw1SEfio9vPy`.
2. Move the Vercel cron to `0 19 * * *` (27 min earlier) — edit `vercel.json` `crons` entry.

**Action required if `digests/2026-09-15.json` does not appear within 30 minutes:** Check Vercel runtime logs for `/api/cron/nightly-routine`. Common causes: `GITHUB_PAT_REPO_WRITE` missing or expired, Neon query failure, or GitHub Contents API error.

_deployment ingest unreachable from sandbox_
