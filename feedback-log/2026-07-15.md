# triage: missing digest — Vercel prepare step failed 2026-07-15

Digest file `digests/2026-07-15.json` was not present when this agent ran at ~03:32 Bali time.

**This is now 9 consecutive days of missing digests (2026-07-07 through 2026-07-15).** This is a persistent Vercel-side failure, not a one-off. No feedback has been triaged since 2026-07-06.

Most likely cause: `GITHUB_PAT_REPO_WRITE` expired or was revoked in Vercel environment variables. The prepare route (`/api/cron/nightly-routine`) cannot write `digests/*.json` to main without it.

**Recommended action:** Check the `GITHUB_PAT_REPO_WRITE` env var in Vercel → Project → Settings → Environment Variables. Generate a new fine-grained PAT scoped to `Benji-cpu/wordzoo` with `Contents: write` and update the Vercel env. Then manually curl the prepare route to confirm it recovers:

```
curl -H "Authorization: Bearer $CRON_SECRET" https://wordzoo.vercel.app/api/cron/nightly-routine
```

_deployment ingest unreachable from sandbox_
