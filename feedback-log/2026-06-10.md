# triage: missing digest — Vercel prepare step failed 2026-06-10

The digest file `digests/2026-06-10.json` was not present in the repo when this agent ran at 03:32 Bali time. The Vercel cron job at `/api/cron/nightly-routine` (scheduled 03:27 Bali) either did not fire or errored before writing the digest.

Check Vercel runtime logs for the `nightly-routine` cron run. Most common causes: `GITHUB_PAT_REPO_WRITE` missing/expired/wrong scope, or a Neon query failure. Curl the route manually with `Authorization: Bearer $CRON_SECRET` to inspect `errors[]` and `digestWritten` in the response.
