# triage: missing digest — Vercel prepare step failed 2026-05-15

`digests/2026-05-15.json` was not present in the repo when the nightly-routine agent ran at 03:32 Bali time. The Vercel cron prepare step (`/api/cron/nightly-routine`, scheduled 03:27 Bali) either did not fire or errored before writing the digest.

Check Vercel runtime logs for the `nightly-routine` cron invocation. Most common causes: `GITHUB_PAT_REPO_WRITE` missing/expired, Neon connectivity issue, or the cron job was skipped. A 401 from a manual curl against that route (with `Authorization: Bearer $CRON_SECRET`) is the green signal that the route is deployed; a 5xx points to a runtime error.

_deployment ingest unreachable from sandbox_
