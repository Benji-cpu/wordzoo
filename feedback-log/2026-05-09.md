# triage: missing digest — Vercel prepare step failed 2026-05-09

The Vercel-side `/api/cron/nightly-routine` prepare step did not produce `digests/2026-05-09.json` before this agent fired. No feedback data is available to triage.

Check Vercel runtime logs for the `nightly-routine` cron function at ~19:27 UTC. Common causes: `GITHUB_PAT_REPO_WRITE` missing/expired/wrong scope in Vercel envs, Neon outage, or the route threw a top-level error. The route uses `safe()` wrappers so partial failures return 200 with non-empty `errors[]` — a hard 500 means an uncaught throw.

_deployment ingest unreachable from sandbox_
